# backend/db — SQL files

- `schema.sql` — full DDL (tables, constraints, indexes). The single source of truth for the database structure. Re-runnable.
- `seed.sql` — demo data for development/demo purposes.

Apply with:
```
python scripts/setup_db.py
```
